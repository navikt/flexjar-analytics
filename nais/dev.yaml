apiVersion: nais.io/v1alpha1
kind: Application
metadata:
  name: flexjar-analytics
  namespace: team-esyfo
  labels:
    team: team-esyfo
spec:
  image: {{image}}
  port: 3000
  replicas:
    min: 1
    max: 2
  liveness:
    path: /api/internal/isAlive
    initialDelay: 10
  readiness:
    path: /api/internal/isReady
    initialDelay: 10
  resources:
    limits:
      memory: 512Mi
    requests:
      cpu: 100m
      memory: 256Mi
  ingresses:
    - https://flexjar-analytics.intern.dev.nav.no
  azure:
    application:
      enabled: true
      allowAllUsers: true
      tenant: trygdeetaten.no
    sidecar:
      enabled: true
      autoLogin: true
      autoLoginIgnorePaths:
        - /api/internal/*
  accessPolicy:
    outbound:
      rules:
        - application: flexjar-analytics-api
  env:
    - name: NODE_ENV
      value: production
    - name: FLEXJAR_BACKEND_URL
      value: http://flexjar-analytics-api
    - name: FLEXJAR_BACKEND_AUDIENCE
      value: api://dev-gcp.team-esyfo.flexjar-analytics-api/.default
