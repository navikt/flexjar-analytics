apiVersion: nais.io/v1alpha1
kind: Application
metadata:
  name: flexjar-analytics-demo
  namespace: team-esyfo
  labels:
    team: team-esyfo
spec:
  image: {{image}}
  port: 3000
  replicas:
    min: 1
    max: 2
  liveness:
    path: /api/internal/isAlive
    initialDelay: 20
  readiness:
    path: /api/internal/isReady
    initialDelay: 20
  resources:
    limits:
      memory: 1024Mi
    requests:
      cpu: 200m
      memory: 512Mi
  ingresses:
    - https://flexjar-analytics-demo.ekstern.dev.nav.no

  env:
    - name: NODE_ENV
      value: production
    - name: USE_MOCK_DATA
      value: "true"
